pipeline {
    agent {
        label('terraform-agent')
    }
    stages {
        stage('Build') {
            steps {
                dir('terraform') {
                    sh 'terraform apply'
                }
            }
        }
    }
}

