pipeline {
    agent {
        label('terraform-agent')
    }
    
    options {
        timeout(time: 10, unit: 'SECONDS')
    }

    stages{
        
        stage("One"){
            //sleep 10
            echo 'hello'
        }
    }
}

