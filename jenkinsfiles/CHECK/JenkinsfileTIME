pipeline {
    agent {
        label('terraform-agent')
    }
    
    options {
        timeout(time: 10, unit: 'SECONDS')
    }

    stages{
        
        stage("One"){
            
            steps{
                sh 'echo hello'
            }
            
        }
    }
}

