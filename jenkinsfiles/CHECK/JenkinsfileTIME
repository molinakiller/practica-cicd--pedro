pipeline {
    agent {
        label('terraform-agent')
    }
    
    options {
        timeout(time: 5, unit: 'MINUTES')
    }

    stages{
        
        stage("One"){
            
            steps{
                sh 'echo hello'
            }
            
        }
    }
}

