pipeline {
    agent {
        label('terraform-agent')
    }
    
    triggers {
        cron('*/10 * * * *')
    }

    stages{
        
        stage("One"){
            
            steps{
                sh 'echo hello'
            }
            
        }
    }
}

